eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
    prefer-ip-address: true
    
spring:
  zipkin:
    base-url: http://localhost:9411/

welcome:
  message: this is comming from config server git repo configured in remote repository
